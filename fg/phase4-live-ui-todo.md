# Phase 4 Live UI Todo

1. Audit current public match detail and existing bet placement flow.
2. Add strict TypeScript payload types for Phoenix live channel events.
3. Add a lightweight Phoenix websocket/channel client for match topics.
4. Build a Zustand live match store using normalized odds rows keyed by id.
5. Implement LiveScoreboard and MarketBoard for in-play cricket layout.
6. Implement RateField with CSS-only 800ms red/green flash and suspension lock.
7. Implement LiveBetSlip with stale quote and market suspension recovery.
8. Integrate the live dashboard into the match detail page without breaking pre-match pages.
9. Verify with eslint.
